#!/bin/sh

BUILD_DIR=$1
CACHE_DIR=$2
ENV_DIR=$3
BUILDPACK_DIR=$(cd "$(dirname "$0")"; cd ..; pwd)

sh $BUILDPACK_DIR/bin/signalfx-agent.sh --realm us0 $(cat "$3/SFX_API_TOKEN") --build_dir $BUILD_DIR --cache_dir $CACHE_DIR --env_dir $ENV_DIR
